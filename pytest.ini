[pytest]
testpaths = tests
addopts = -m "not integration" -k "not (test_server_config or test_stock_codes)"
markers =
    integration: 标记集成/端到端测试（默认跳过）
    smoke: 标记适合在 CI 中快速回归的测试
    acceptance: 验收测试（需要后端服务运行，连接真实数据库）